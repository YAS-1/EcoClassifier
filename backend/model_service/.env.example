# Flask server
FLASK_HOST=0.0.0.0
FLASK_PORT=5001
FLASK_DEBUG=false

# Model & thresholds
MODEL_PATH=./models/best.pt

# Decision thresholds (tweak these)
PAPER_THRESHOLD=0.30
PLASTIC_THRESHOLD=0.30
GLOBAL_MIN=0.25
CONFIDENCE_MARGIN=0.08

# Optional: simple API key to protect endpoint (set blank to disable)
MODEL_API_KEY=
